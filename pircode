void setup() {
  pinMode(13, OUTPUT);
  pinMode(5, INPUT);
  Serial.begin(9600);
}

void loop() {
  int value;
  value = digitalRead(5);
  Serial.println(value);
  if (value == 1) {
    digitalWrite(13, HIGH);
  } else {
    digitalWrite(13, LOW);
  }
}
